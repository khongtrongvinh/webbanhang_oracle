@model IEnumerable<WepSiteBanHang.Models.LoaiSanPham>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/LayoutAD/AdinLayout.cshtml";
}



<p>
    @Html.ActionLink("Thêm loại sản phẩm", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("Tên loại")
        </th>
       
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoai)
        </td>
        
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaLoaiSP }) |
            @Html.ActionLink("Xem chi tiết", "Details", new { id=item.MaLoaiSP }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaLoaiSP })
        </td>
    </tr>
}

</table>
